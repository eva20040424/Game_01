:: StoryInit

/* 1. 角色名稱設定 */
<<set $name to "">> /* 預設本名 */
<<set $nick to "">> /* 預設暱稱 */

/* 2. 身體數值設定 */
<<set $bladder to 0>> /* 膀胱容量 */
<<set $money to 100>> /* 金錢 */

/* 3. 時間系統 */
<<set $startYear to 2025>> /* 借用2025但仍顯示第1年 */
<<set $gameDate to new Date("2025-08-31T07:00:00")>>

/* 4. AI 連結設定 */
<<set $apiKey to "">> /* 當前使用的金鑰 */
<<set $keyVault to []>> /* 金鑰儲存庫 */
<<set $aiModel to "gemini-3-flash-preview">> /* 預設模型 */
<<set $systemPrompt to "">> /* 系統指令/破限詞 */

/* === 系統開關與狀態欄初始值 === */
<<set $enable_ai_status to false>>

/* 數值設為 0 或 初始狀態 */
<<set $sys_bladder_ml to 0>>
<<set $sys_bladder_state to "正常">>
<<set $sys_womb_depth to 0>>
<<set $sys_womb_state to "閉合">>
<<set $sys_anal_state to "正常">>

/* 描寫設為 空字串 */
<<set $ai_desc_bladder to "">>
<<set $ai_desc_womb to "">>
<<set $ai_desc_anal to "">>