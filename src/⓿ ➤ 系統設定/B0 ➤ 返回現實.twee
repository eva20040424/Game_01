:: 返回現實
/* 說明：這是一個「中轉站」，玩家不會看到這個頁面。
    它負責讀取變數，把玩家送回原本的地方。
*/

/* 檢查變數是否存在 (防止報錯) */
<<if def $lastRealWorldPassage>>
    /* 讀取記憶的座標，傳送回去 */
    <<goto $lastRealWorldPassage>>
<<else>>
    /* 如果找不到回去的路 (例如直接從測試模式啟動遊戲)，預設傳送到首頁 */
    <<goto "Start">>
<</if>>